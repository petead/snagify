/**
 * Single entry for image / capture dimension helpers used by inspection flows.
 */
export {
  assertValidDimensions,
  getImageDimensions,
  getPdfImageHeight,
  getVideoCaptureDimensions,
  isLandscape,
  type ImageDimensions,
} from "./photos/getImageDimensions";
